{% props
    as = "span",
    content = null,
%}

<{{ as }}
    {{ attributes
        .defaults({
            class: "step-icon",
        })
    }}
>
    {% block content %}{% if content is not null %}{{ content }}{% endif %}{% endblock %}
</{{ as }}>
