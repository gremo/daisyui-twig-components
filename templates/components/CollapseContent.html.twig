{% props
    as = "div",
    content = null,
%}

<{{ as }}
    {{ attributes
        .defaults({
            class: "collapse-content",
        })
    }}
>
    {% block content %}{% if content is not null %}{{ content }}{% endif %}{% endblock %}
</{{ as }}>
